#!/usr/bin/env php

<?php

require(__DIR__ . '/../src/docopt.php');

$autoloadPath1 = __DIR__ . '/../../../autoload.php';
$autoloadPath2 = __DIR__ . '/../vendor/autoload.php';
if (file_exists($autoloadPath1)) {
    require_once $autoloadPath1;
} else {
    require_once $autoloadPath2;
}

$params = array(
    'help'=>true,
    'version'=>'Gendiffer 0.3',
    //'optionsFirst'=>false,
);

/* $handler = new \Docopt\Handler(array(
    'help'=>true,
    'version'=>'gendiff version 0.3',
    'format'=>genDiff($firstFile, $secondFile),
    'optionsFirst'=>false,
)); */

//$args = $handler->handle($doc, $argv);

$args = Docopt::handle($doc, $params);
foreach ($args as $key => $value)
    //echo $key.': '.json_encode($value).PHP_EOL;
    echo $key . " => " . $value . "\n";

//print_r($args['format']);
